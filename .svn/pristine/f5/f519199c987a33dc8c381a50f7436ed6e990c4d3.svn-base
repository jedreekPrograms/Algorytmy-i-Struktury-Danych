#ifndef  STACK_H
#define STACK_H

#include "queue.h"

typedef struct {
    Node* top;
} Stack;

void initStack(Stack* S);
int isEmptyStack(Stack* S);
void push(Stack* S, int value);
int pop(Stack* S);

#endif